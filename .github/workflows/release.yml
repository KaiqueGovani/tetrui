name: Build and Release

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  build:
    environment: actions
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.runner }}
    env:
      TETRUI_SCORE_API_URL: ${{ secrets.TETRUI_SCORE_API_URL }}
      TETRUI_SCORE_API_KEY: ${{ secrets.TETRUI_SCORE_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Install audio deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libasound2-dev

      - name: Build
        shell: bash
        run: |
          mkdir -p dist
          GOOS=$(go env GOOS)
          GOARCH=$(go env GOARCH)
          echo "GOOS=$GOOS" >> $GITHUB_ENV
          echo "GOARCH=$GOARCH" >> $GITHUB_ENV
          EXT=""
          if [ "$GOOS" = "windows" ]; then
            EXT=".exe"
          fi
          go build -trimpath -buildvcs=false -ldflags "-s -w -X main.defaultScoreAPIURL=${TETRUI_SCORE_API_URL} -X main.defaultScoreAPIKey=${TETRUI_SCORE_API_KEY}" -o "dist/tetrui-${GOOS}-${GOARCH}${EXT}" .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tetrui-${{ env.GOOS }}-${{ env.GOARCH }}
          path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Publish nightly release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: Nightly
          prerelease: true
          files: dist/**
